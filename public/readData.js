"use strict";

function readInDataItem(itemName, fileLoadedCallback){
  // TODO iterate through each directory and load all
  // files inside
  // d3.csv("data/<filename.csv>").then(fileLoadedCallback);
}